{
  "name": "infinity-new-server",
  "version": "1.0.0",
  "private": true,
  "description": "Root entry so hosts (Render) can build/start the API from the repo root.",
  "type": "module",
  "scripts": {
    "start": "node server/src/index.mjs"
  },
  "engines": {
    "node": ">=18 <=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "firebase-admin": "^13.0.0",
    "pg": "^8.13.0"
  }
}
